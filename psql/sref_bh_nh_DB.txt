CREATE TABLE sref.bh_nh(
	line   TEXT,
	trace   INT,
	lat   FLOAT,
	long   FLOAT,
	sza   FLOAT,
	sref   FLOAT
);

CREATE INDEX idx_loc_bh_nh_sref ON sref.bh_nh (lat,lon);
CREATE INDEX idx_line_bh_nh_sref ON sref.bh_nh (line);
CREATE INDEX idx_sza_bh_nh_sref ON sref.bh_nh (sza);
